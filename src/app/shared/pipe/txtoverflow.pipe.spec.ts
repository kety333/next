import { TxtoverflowPipe } from './txtoverflow.pipe';

describe('TxtoverflowPipe', () => {
  it('create an instance', () => {
    const pipe = new TxtoverflowPipe();
    expect(pipe).toBeTruthy();
  });
});
